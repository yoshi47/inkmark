{
  "name": "inkmark",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -b",
    "build:server": "tsc -p tsconfig.build.json",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.9.1",
    "typescript": "^5.9.0"
  }
}
